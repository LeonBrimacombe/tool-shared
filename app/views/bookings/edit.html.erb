<div class="container">
  <h1>Amending booking #<%= @booking.id %> for <%= @tool.name.upcase%></h1>

  <%= simple_form_for[@tool, @booking] do |f| %>
     <%= f.input :start_date %>
     <%= f.input :end_date %>
    <%= f.submit class: 'btn-submit mt-3'%>
  <% end %>
</div>
