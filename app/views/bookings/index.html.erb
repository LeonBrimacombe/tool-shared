<h1>Your bookings</h1>

<div class="container">
  <div class="cards">
    <% @bookings.each do |booking| %>
    <div class="booking-card">
      <h5>Booking #<%= booking.id %></h5>
      Start date: <%= booking.start_date %> <br>
      End date: <%= booking.end_date %> <br>
      Price: £<%= booking.price %> per hour <br>
    </div>
    <% end %>
  </div>
</div>
